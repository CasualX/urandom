[package]
name = "urandom-wasm-tests"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
urandom = { path = "..", default-features = false }

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
